apiVersion: v1
kind: Service
metadata:
  name: kk-payments
  labels:
    app: kk-payments
spec:
  selector:
    app: kk-payments
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
