apiVersion: v1
kind: ConfigMap
metadata:
  name: kk-payments-config
data:
  DB_HOST: "kk-payments-db.default.svc.cluster.local"
  ENVIRONMENT: "production"
  RECEIPTS_BUCKET: "kk-payments-receipts-production"
  LOG_LEVEL: "info"
